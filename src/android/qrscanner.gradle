repositories {
    jcenter()
}

dependencies {
    compile 'com.journeyapps:zxing-android-embedded:3.3.0'
    compile 'com.android.support:appcompat-v7:26.1.0'
}

android {
    buildToolsVersion '27.0.1' // Older versions may give compile errors
}
